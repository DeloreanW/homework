let pizzaPlace = "PizzaPalace";
let numberOfToppings = 5;

console.log(pizzaPlace, typeof pizzaPlace);
console.log(numberOfToppings, typeof numberOfToppings);

let chicago = `we have the best pizza on the planet here at ${pizzaPlace} where we have the best ingredients with ${numberOfToppings} number of different toppings `;
console.log(chicago);
if (numberOfToppings < 10);
{
  console.log("Quality, not quantity");
}
